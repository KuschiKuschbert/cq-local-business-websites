# `/council-registry`

Run `python3 .agent/skills/growth-engine/scripts/generate-council-registry.py` to list which council owns each decision type in the business engine.

The registry routes decisions only. It does not approve promotions, send outreach, publish sites, create GitHub issues, change hosting, or bill clients.
