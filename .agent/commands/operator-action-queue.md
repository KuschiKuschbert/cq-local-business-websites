# `/operator-action-queue`

Run `python3 .agent/skills/growth-engine/scripts/generate-operator-action-queue.py` to produce one ranked queue of safe next actions, Daniel decisions, planned operations, and blocked external actions.

The queue is advisory only. It cannot approve prospect promotion, outreach, publishing, remote GitHub writes, billing, or client-facing claims.
