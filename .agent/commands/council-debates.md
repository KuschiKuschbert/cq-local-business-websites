# `/council-debates`

Run `python3 .agent/skills/growth-engine/scripts/generate-council-debates.py` to produce current council arguments before major business-engine decisions.

Each debate includes best case, hard pushback, council split, verdict, next test, evidence path, and safety gate. Council verdicts are advisory unless Daniel separately approves the gated action.
