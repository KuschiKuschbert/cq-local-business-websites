# `/research-suppression-list`

Run `python3 .agent/skills/growth-engine/scripts/generate-research-suppression-list.py`.

This generates an advisory list of repeated failed public research patterns. It prevents the engine from repeating weak searches, but it does not approve capture, promotion, outreach, publishing, billing, or remote GitHub writes.
