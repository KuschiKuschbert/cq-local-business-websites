# `/source-pivot-plan`

Run `python3 .agent/skills/growth-engine/scripts/generate-source-pivot-plan.py` to produce alternate research routes for candidates whose named social searches failed repeatedly.

The pivot plan suggests source-hint directory, official-site, tourism, council, or local arts queries. It is research-only and cannot approve capture, promotion, outreach, publishing, remote GitHub writes, billing, or client-facing actions.
